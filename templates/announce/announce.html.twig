{% extends 'base.html.twig' %}

{% block title %}
    Ajouter une annonce - {{parent()}}
{% endblock %}

{% block body %}

<section class="container text-center">
    <div class="row py-lg-5">
        <div class="col-lg-6 col-md-8 mx-auto">
            <h1 class="fw-light">Ajouter une annonce</h1>
        </div>
    </div>
</section>

<div class="input-group-sm mb-3 mx-auto" style="width: 450px;">
    {{ form_start(formView, {'attr': {'novalidate': 'novalidate'}}) }}
        
    {{ form_end(formView) }}
</div>  

<a href=" {{ path('home') }} " class="btn btn-secondary my-2 mx-4">Retour à l'accueil</a>  

{% endblock %}
