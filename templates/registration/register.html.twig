{% extends 'base.html.twig' %}

{% block title %}
    S'enregistrer - {{parent()}} 
{% endblock %}

{% block body %}
<section class="py-5  container">
    <div class="form-signin py-5">
        <h1 class="h3 mb-3 fw-bold fst-italic text-center">Inscription</h1>

        <div class="form-floating ">
        {{ form_start(registrationForm) }}
        </div>
            <div class="form-floating my-2">
            {{ form_widget(registrationForm.email) }}
            {{ form_label(registrationForm.email) }}
             </div>
            <div class="form-floating my-2">
            {{ form_widget(registrationForm.lastname)}}
            {{ form_label(registrationForm.lastname)}}
            </div>
            <div class="form-floating my-2">
            {{ form_widget(registrationForm.firstname)}}
            {{ form_label(registrationForm.firstname)}}
            </div>
            <div class="form-floating my-2">
            {{ form_widget(registrationForm.adress)}}
            {{ form_label(registrationForm.adress)}}
            </div>
            <div class="form-floating my-2">
            {{ form_widget(registrationForm.phone)}}
            {{ form_label(registrationForm.phone)}}
            </div>
            <div class="form-floating my-2">
            {{ form_widget(registrationForm.agreeTerms) }}
            {{ form_label(registrationForm.agreeTerms) }}
            </div>
        

            <button type="submit" class="w-100 btn btn-lg btn-secondary">S'inscrire</button>
        {{ form_end(registrationForm) }}
        </div>
    </section>
</main>
{% endblock %}
